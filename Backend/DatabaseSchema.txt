-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.course_offering_mth_rooms (
  offering_id integer NOT NULL,
  room_id integer NOT NULL,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT course_offering_mth_rooms_pkey PRIMARY KEY (offering_id, room_id),
  CONSTRAINT course_offering_mth_rooms_offering_id_fkey FOREIGN KEY (offering_id) REFERENCES public.course_offerings(id),
  CONSTRAINT course_offering_mth_rooms_room_id_fkey FOREIGN KEY (room_id) REFERENCES public.rooms(room_id)
);
CREATE TABLE public.course_offering_tfs_rooms (
  offering_id integer NOT NULL,
  room_id integer NOT NULL,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT course_offering_tfs_rooms_pkey PRIMARY KEY (offering_id, room_id),
  CONSTRAINT course_offering_tfs_rooms_offering_id_fkey FOREIGN KEY (offering_id) REFERENCES public.course_offerings(id),
  CONSTRAINT course_offering_tfs_rooms_room_id_fkey FOREIGN KEY (room_id) REFERENCES public.rooms(room_id)
);
CREATE TABLE public.course_offerings (
  curr_id integer NOT NULL,
  code text,
  course_no text,
  department_id integer,
  section text,
  descriptive_title text,
  units real,
  lec_hrs real,
  lab_hrs real,
  mth_schedule text,
  mth_room_id text CHECK (mth_room_id IS NULL OR mth_room_id ~ '^[0-9]+(\/[0-9]+)*$'::text),
  tfs_schedule text,
  tfs_room_id text CHECK (tfs_room_id IS NULL OR tfs_room_id ~ '^[0-9]+(\/[0-9]+)*$'::text),
  merged text DEFAULT false,
  id integer NOT NULL DEFAULT nextval('course_offerings_id_seq'::regclass),
  CONSTRAINT course_offerings_pkey PRIMARY KEY (id),
  CONSTRAINT course_offerings_department_id_fkey FOREIGN KEY (department_id) REFERENCES public.departments(department_id)
);
CREATE TABLE public.departments (
  department_id integer NOT NULL DEFAULT nextval('departments_department_id_seq'::regclass),
  department_name text NOT NULL,
  department_program text,
  CONSTRAINT departments_pkey PRIMARY KEY (department_id)
);
CREATE TABLE public.faculty (
  faculty_id integer NOT NULL DEFAULT nextval('faculty_faculty_id_seq'::regclass),
  faculty_name text NOT NULL,
  department_id integer,
  faculty_email text,
  faculty_specialization text,
  faculty_max_units integer,
  faculty_role text,
  faculty_status text,
  CONSTRAINT faculty_pkey PRIMARY KEY (faculty_id),
  CONSTRAINT faculty_department_id_fkey FOREIGN KEY (department_id) REFERENCES public.departments(department_id)
);
CREATE TABLE public.faculty_loading (
  facloading_id integer NOT NULL DEFAULT nextval('faculty_loading_facloading_id_seq'::regclass),
  curr_id integer,
  faculty_id integer,
  code text,
  course_no text,
  department_id integer,
  section text,
  descriptive_title text,
  units integer,
  lec_hrs integer,
  lab_hrs integer,
  mth_schedule text,
  mth_room_id integer,
  tfs_schedule text,
  tfs_room_id integer,
  merged boolean DEFAULT false,
  CONSTRAINT faculty_loading_pkey PRIMARY KEY (facloading_id),
  CONSTRAINT faculty_loading_faculty_id_fkey FOREIGN KEY (faculty_id) REFERENCES public.faculty(faculty_id),
  CONSTRAINT faculty_loading_department_id_fkey FOREIGN KEY (department_id) REFERENCES public.departments(department_id),
  CONSTRAINT faculty_loading_mth_room_id_fkey FOREIGN KEY (mth_room_id) REFERENCES public.rooms(room_id),
  CONSTRAINT faculty_loading_tfs_room_id_fkey FOREIGN KEY (tfs_room_id) REFERENCES public.rooms(room_id)
);
CREATE TABLE public.rooms (
  room_id integer NOT NULL DEFAULT nextval('rooms_room_id_seq'::regclass),
  room_name text NOT NULL,
  room_type text,
  room_status text,
  CONSTRAINT rooms_pkey PRIMARY KEY (room_id)
);
CREATE TABLE public.subjects (
  subject_id integer NOT NULL DEFAULT nextval('subjects_subject_id_seq'::regclass),
  subject_code text,
  subject_course_no text,
  subject_name text,
  department_id integer,
  subject_section text,
  subject_descriptive_title text,
  subject_units integer,
  subject_lec_hrs integer,
  subject_lab_hrs integer,
  subject_schedule text,
  room_id integer,
  subject_status text,
  CONSTRAINT subjects_pkey PRIMARY KEY (subject_id),
  CONSTRAINT subjects_department_id_fkey FOREIGN KEY (department_id) REFERENCES public.departments(department_id),
  CONSTRAINT subjects_room_id_fkey FOREIGN KEY (room_id) REFERENCES public.rooms(room_id)
);
CREATE TABLE public.users (
  user_id integer NOT NULL DEFAULT nextval('users_user_id_seq'::regclass),
  username text NOT NULL UNIQUE,
  email text,
  password_hash text NOT NULL,
  role text DEFAULT 'staff'::text,
  status text DEFAULT 'active'::text,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT users_pkey PRIMARY KEY (user_id)
);